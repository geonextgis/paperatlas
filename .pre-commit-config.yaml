repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-toml
      - id: check-yaml
        exclude: ^paperatlas/configs/
      - id: check-added-large-files
        args: ["--maxkb=500"]
